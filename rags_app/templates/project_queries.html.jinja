{% extends "skeleton.html.jinja" %}
{% set current_page = "projects" %}
{% block title %}Project Queries{% endblock %}
{% block main %}
<div class="projects_body">
   {% if error_message %}
   <div class="alert alert-danger" role="alert">{{ error_message }}</div>
   {% endif %}
   {% if success_message %}
   <div class="alert alert-success" role="alert">{{ success_message }}</div>
   {% endif %}
   {% if not project_query %}
   <div class="card border-dark mb-3">
     <h3 class="card-header">Query the graph for Project - {{ project.name }}</h3>
     <div class="card-body">
        <div class="row">
          <div class="col-md-4">
            <h2>GWAS</h2>
            <p>View the most significant GWAS results:</p>
            <p><a class="btn btn-secondary" href="/project_query/{{ project.id }}/1" role="button">View results &raquo;</a></p>
          </div>
          <div class="col-md-4">
            <h2>MWAS</h2>
            <p>View the most significant MWAS results:</p>
            <p><a class="btn btn-secondary" href="/project_query/{{ project.id }}/2" role="button">View details &raquo;</a></p>
          </div>
          <!--
          <div class="col-md-4">
            <h2>All The AS</h2>
            <p>Donec sed odio dui. Cras justo odio, dapibus ac facilisis in, egestas eget quam. Vestibulum id ligula porta felis euismod semper. Fusce dapibus, tellus ac cursus commodo, tortor mauris condimentum nibh, ut fermentum massa justo sit amet risus.</p>
            <p><a class="btn btn-secondary" href="#" role="button">View details &raquo;</a></p>
          </div>-->
        </div>
     </div>
  </div>
  {% else %}
  <div class="alert alert-info" role="alert">Results for the query: {{ project_query }}</div>
  <table class="table table-striped table-sm">
  <thead>
    <tr>
    {% for q_header in project_query_headers %}
       <th>{{ q_header }}</th>
    {% endfor %}
    </tr>
  </thead>
  <tbody>
  {% for result in project_query_results %}
  <tr>
    {% for subresult in result %}
    <td>{{ subresult }}</td>
    {% endfor %}
  </tr>
  {% endfor %}
  </tbody>
  </table>
  {% endif %}
</div>
{% endblock %}